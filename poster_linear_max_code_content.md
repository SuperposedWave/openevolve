# 面向线性码与最大码问题的多模态可信大模型研究

## 课题名称

面向线性码与最大码问题的多模态可信大模型研究

副标题：基于 OpenEvolve / FunSearch 范式的编码理论组合优化自动发现

## 课题简介

本课题面向编码理论中的两类经典困难问题：二元线性码可行性搜索与二元最大码搜索。线性码部分关注构造满足给定参数 `[n,k,d]` 的二元线性码；最大码部分关注在长度 `n`、最小汉明距离 `d` 约束下，寻找尽可能大的二元码 `C`，即提升 `A(n,d)` 的可行下界。

项目采用“可信大模型 + 精确验证器”的协同模式：大模型只负责生成或改进轻量级启发式评分函数，固定搜索器负责候选枚举、合法性判断、矩阵构造、距离验证与指标输出。这样既利用大模型探索复杂组合空间的能力，又通过精确数学验证保证结果可信。

核心方法包括：

- 对线性码，构造系统形式校验矩阵 `H = [P^T | I_r]`，其中 `r=n-k`，搜索 `k` 个自由列，使任意 `d-1` 列线性无关。
- 对最大码，搜索满足任意两码字汉明距离至少为 `d` 的大码集；对 `d=4` 情形使用 `A_2(n,4)=A_2(n-1,3)` 的奇偶扩展变换。
- 使用 OpenEvolve 进化 `priority(...)`、`destroy_priority(...)`、`repair_priority(...)` 等启发式函数，并由独立评估器给出可复现的分数与构造证据。

## 课题进展

目前项目已形成可运行、可验证、可视化的完整实验闭环。

- 已实现二元线性码搜索示例 `examples/linear_code_binary_search`，支持 Python 启发式、C 优先级函数、批量运行和矩阵验证。
- 已实现二元最大码搜索示例 `examples/max_binary_code_search`，可输出码字集合、验证最小距离，并以最终合法码字数作为优化分数。
- 在线性码默认实例 `[8,4,4]` 上，baseline 可成功构造完整码，`combined_score=1.0`，构造 4 个自由列，并导出 `H` 与 `G` 矩阵。
- 在最大码默认实例 `A(17,4)` 上，baseline 验证得到合法码，码字数为 `1991`，最小距离为 `4`。
- 二元线性码搜索已收敛到 C kernel 路径：动态窗口重排与局部 repair 作为固定搜索骨架，Python 搜索路径已移除。
- 已实现可选原生 C 合法性引擎 `_linear_code_native`，支持 `r <= 60`，用于加速精确 forbidden-state 查询、更新、回退与克隆。
- 已生成线性码表可视化数据：共覆盖 `32896` 个 `(n,k)` 单元，其中 `2457` 个标记为 found，`142` 个标记为 searched-not-found，`35` 个为 upper failed。
- 已有回归测试覆盖合法性引擎、采样搜索、C kernel 评估、矩阵正交性、viewer 数据生成和默认实例成功构造。

## 研究方向

### 方向一：可信启发式自动发现

研究如何让大模型只生成局部评分函数，而不直接改写数学约束与验证流程。重点探索列权重、支撑平衡、bit-run 结构、forbidden-set 增量、局部可用性等特征组合，使进化出的启发式更稳定、更可解释。

### 方向二：大规模码参数搜索

针对较大 `n,k,d`，完整候选枚举和排序成本快速上升。后续重点优化抽样补池、束搜索、动态窗口重排、restart 并行与 C 原生合法性引擎，使系统能够覆盖更大参数区间并发现新的下界证据。

### 方向三：线性码与最大码的统一建模

线性码搜索侧重矩阵列独立性，最大码搜索侧重码字间汉明距离。两者都可抽象为“候选选择 + 精确冲突约束 + 启发式排序”的组合优化框架。后续可统一候选特征、冲突图表示、局部 repair 策略和可视化分析工具。

### 方向四：多模态结果理解与展示

将搜索过程、码表状态、校验矩阵、生成矩阵、码字分布和演化轨迹转化为可视化材料，辅助研究者理解大模型发现的结构规律。海报、矩阵 viewer、码表热力图和代码演化图可共同构成多模态解释界面。

## 课题亮点

- 可信：大模型只提出启发式，所有构造由固定评估器精确验证，避免“看似合理但不可验证”的结果。
- 可复现：目标参数、随机种子、restart 数、搜索模式均通过环境变量或配置文件控制，便于重复实验。
- 可扩展：同时支持 Python 快速实验和 C kernel 高性能路径，并提供 native legality engine 加速大规模实例。
- 可解释：每次成功构造可输出选中列、候选排名、权重直方图、`H`/`G` 矩阵和搜索指标。
- 可视化：码表 viewer 可展示 `(n,k)` 单元状态、已知上下界、尝试目标和对应优先级来源。
- 可迁移：同一 FunSearch / OpenEvolve 范式可迁移到其他离散数学与组合优化问题，如 packing、covering、图码与有限几何构造。

## 作者介绍

作者名：请填写作者姓名

研究方向：可信大模型、组合优化、编码理论、自动化算法发现

项目角色：负责线性码与最大码问题建模、OpenEvolve 评估器设计、启发式搜索实验、结果验证与可视化整理。

个人简介：围绕“用大模型辅助发现可验证数学结构”开展研究，关注大模型生成能力与形式化/程序化验证机制的结合，希望构建可复现、可解释、可扩展的自动科研工具链。

## 联系方式

邮箱：请填写邮箱

单位：请填写学院/实验室/课题组

GitHub / 项目地址：请填写项目仓库链接

二维码建议：放置项目仓库、实验结果 viewer 或个人主页二维码。

## 海报配图建议

- 课题简介区：放 `H=[P^T|I_r]` 矩阵结构示意图，突出自由列搜索与固定单位阵。
- 课题进展区：放线性码表 viewer 截图或 found/failed 统计图。
- 研究方向区：放流程图：大模型生成 priority -> 固定搜索器构造 -> 精确验证 -> 反馈进化。
- 课题亮点区：放 `H`/`G` 矩阵样例、码字分布图或 OpenEvolve 演化轨迹图。

## 参考项目文件

- `examples/linear_code_binary_search/README.md`
- `examples/linear_code_binary_search/search_core.py`
- `examples/linear_code_binary_search/initial_program.c`
- `examples/max_binary_code_search/README.md`
- `examples/max_binary_code_search/search_core.py`
- `examples/max_binary_code_search/initial_program.py`
- `examples/linear_code_binary_search/code_table_viewer/code_table_data.json`
- `docs/superpowers/specs/2026-05-06-sampled-refill-greedy-design.md`
