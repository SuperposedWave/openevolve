{"id": "b8862967-101e-43fb-a375-d0cbcfe808c9", "code": "\"\"\"Baseline static priority heuristic for FunSearch-style ternary matrix construction.\"\"\"\n\n\n# EVOLVE-BLOCK-START\ndef _row_count(n, k):\n    return n - k\n\n\ndef _target_weight(n, k, d):\n    r = _row_count(n, k)\n    return max(d - 1, r // 2 + 1)\n\n\ndef _decode_column(column_code, r):\n    digits = []\n    value = column_code\n    for _ in range(r):\n        digits.append(value % 3)\n        value //= 3\n    return digits\n\n\ndef _support_weight(digits):\n    return sum(1 for digit in digits if digit)\n\n\ndef _symbol_balance_score(digits):\n    ones = digits.count(1)\n    twos = digits.count(2)\n    return -abs(ones - twos)\n\n\ndef _nonzero_span_score(digits):\n    first = -1\n    last = -1\n    for index, digit in enumerate(digits):\n        if digit:\n            if first < 0:\n                first = index\n            last = index\n    if first < 0:\n        return 0\n    return last - first\n\n\ndef priority(column_code, n, k, d):\n    \"\"\"\n    Score a candidate free column using only static instance information.\n\n    The evaluator keeps legality exact; this function only defines a global ranking.\n    \"\"\"\n    r = _row_count(n, k)\n    digits = _decode_column(column_code, r)\n    weight = _support_weight(digits)\n    target_weight = _target_weight(n, k, d)\n\n    weight_score = -abs(weight - target_weight)\n    balance_score = _symbol_balance_score(digits)\n    span_score = _nonzero_span_score(digits)\n    endpoint_bonus = 0.5 * (digits[0] != 0) + 0.5 * (digits[-1] != 0)\n\n    return (\n        2.5 * weight_score\n        + 0.5 * balance_score\n        + 0.2 * span_score\n        + endpoint_bonus\n    )\n\n\n# EVOLVE-BLOCK-END\n\nfrom search_core import DEFAULT_INSTANCE, evaluate_priority_function, instance_from_env\n\n\ndef get_priority_function():\n    \"\"\"Stable entry point for tests and manual inspection.\"\"\"\n    return priority\n\n\ndef run_baseline_suite():\n    \"\"\"Evaluate the current priority function on one configured instance.\"\"\"\n    return evaluate_priority_function(priority, instance_from_env())\n\n\nif __name__ == \"__main__\":\n    configured_instance = instance_from_env()\n    result = run_baseline_suite()\n    print(\"Default instance:\", DEFAULT_INSTANCE)\n    print(\"Configured instance:\", configured_instance)\n    print(\"Metrics:\", result.metrics)\n    print(\"Artifacts:\", result.artifacts)\n", "changes_description": null, "language": "python", "parent_id": "1ea7227f-387b-4b30-9079-9568324535b4", "generation": 1, "timestamp": 1777288081.423715, "iteration_found": 2, "metrics": {"combined_score": 1.0, "success_rate": 1.0, "avg_progress": 1.0, "constructed_columns": 8, "target_columns": 8, "target_distance": 5, "n": 15, "k": 8, "evaluation_time_seconds": 0.19636562501545995}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  def _center_symmetry_score(digits):\n      r = len(digits)\n      left = 0\n      right = 0\n      for i in range(r // 2):\n          if digits[i]:\n              left += 1\n          if digits[r - 1 - i]:\n              right += 1\n      return -abs(left - right)\n  \n  \n  def _run_count_score(digits):\n      bits = \"\".join(str(digit) for digit in digits)\n      runs = 1\n      for i in range(1, len(bits)):\n          if bits[i] != bits[i - 1]:\n              runs += 1\n      return runs\n  \n  \n  def _symbol_balance_score(digits):\n      ones = digits.count(1)\n      twos = digits.count(2)\n      return -abs(ones - twos)\n  \n  \n  def _nonzero_span_score(digits):\n      first = -1\n      last = -1\n  ... (36 more lines)\nwith:\n  def _symbol_balance_score(digits):\n      ones = digits.count(1)\n      twos = digits.count(2)\n      return -abs(ones - twos)\n  \n  \n  def _nonzero_span_score(digits):\n      first = -1\n      last = -1\n      for index, digit in enumerate(digits):\n          if digit:\n              if first < 0:\n                  first = index\n              last = index\n      if first < 0:\n          return 0\n      return last - first\n  \n  \n  def priority(column_code, n, k, d):\n      \"\"\"\n      Score a candidate free column using only static instance information.\n  \n      The evaluator keeps legality exact; this function only defines a global ranking.\n      \"\"\"\n      r = _row_count(n, k)\n      digits = _decode_column(column_code, r)\n      weight = _support_weight(digits)\n      target_weight = _target_weight(n, k, d)\n  \n  ... (11 more lines)", "parent_metrics": {"combined_score": 1.0, "success_rate": 1.0, "avg_progress": 1.0, "constructed_columns": 8, "target_columns": 8, "target_distance": 5, "n": 15, "k": 8, "evaluation_time_seconds": 0.12849379202816635}, "island": 1}, "prompts": {"diff_user": {"system": "Current target:\n- n = 15\n- k = 8\n- d = 5\n- r = n - k = 7\n\nYou are improving a static scoring function for a ternary GF(3) matrix construction problem.\n\nWe build a ternary parity-check matrix in systematic form:\n\n  H = [P^T | I_r]\n\nHere:\n- `H` is a GF(3) matrix of shape `r x n`\n- `P^T` is the free part and has shape `r x k`\n- `I_r` is fixed\n- the search only chooses the `k` free columns in `P^T`\n- each free column is passed to the priority function as a base-3 encoded integer\n- scalar multiples are normalized projectively, so the first non-zero coordinate is 1\n\nGoal:\nchoose free columns so that any `d - 1` columns of `H` are linearly independent over GF(3).\n\nYou must improve this function only:\n\n  priority(column_code: int, n: int, k: int, d: int) -> float\n\nThis is a FunSearch-style setup:\n- your function scores each candidate column once\n- the evaluator sorts all candidates by that score\n- the fixed greedy skeleton scans that order and keeps every legal column it can add\n\nSuccess criterion:\n- best case: the greedy constructor successfully selects all `k` free columns\n- otherwise: maximize how many legal free columns it can select\n\nRules:\n- keep the same function signature\n- do not use randomness\n- do not import extra libraries\n- do not reimplement legality checks\n- do not try to construct the whole matrix directly\n\nUseful heuristic ideas:\n- avoid GF(3) columns that are too sparse or too dense\n- prefer balanced support across the `r` row positions\n- reward ternary digit patterns that look structurally useful but not overly repetitive\n- consider the balance between symbols 1 and 2\n- prefer columns that are likely to remain compatible with many other columns later\n\nWrite simple, deterministic heuristic logic that ranks promising columns above weaker ones.\n", "user": "# Current Program Information\n- Fitness: 1.0000\n- Feature coordinates: avg_progress=1.00, constructed_columns=8.00\n- Focus areas: - Fitness unchanged at 1.0000\n- Exploring avg_progress=1.00, constructed_columns=8.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 1\n- Changes: Unknown changes\n- Metrics: combined_score: 1.0000, success_rate: 1.0000, avg_progress: 1.0000, constructed_columns: 8.0000, target_columns: 8.0000, target_distance: 5.0000, n: 15.0000, k: 8.0000, evaluation_time_seconds: 0.1285\n- Outcome: Improvement in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: 1.0000)\n```python\n\"\"\"Baseline static priority heuristic for FunSearch-style ternary matrix construction.\"\"\"\n\n\n# EVOLVE-BLOCK-START\ndef _row_count(n, k):\n    return n - k\n\n\ndef _target_weight(n, k, d):\n    r = _row_count(n, k)\n    return max(d - 1, r // 2 + 1)\n\n\ndef _decode_column(column_code, r):\n    digits = []\n    value = column_code\n    for _ in range(r):\n        digits.append(value % 3)\n        value //= 3\n    return digits\n\n\ndef _support_weight(digits):\n    return sum(1 for digit in digits if digit)\n\n\ndef _center_symmetry_score(digits):\n    r = len(digits)\n    left = 0\n    right = 0\n    for i in range(r // 2):\n        if digits[i]:\n            left += 1\n        if digits[r - 1 - i]:\n            right += 1\n    return -abs(left - right)\n\n\ndef _run_count_score(digits):\n    bits = \"\".join(str(digit) for digit in digits)\n    runs = 1\n    for i in range(1, len(bits)):\n        if bits[i] != bits[i - 1]:\n            runs += 1\n    return runs\n\n\ndef _symbol_balance_score(digits):\n    ones = digits.count(1)\n    twos = digits.count(2)\n    return -abs(ones - twos)\n\n\ndef _nonzero_span_score(digits):\n    first = -1\n    last = -1\n    for index, digit in enumerate(digits):\n        if digit:\n            if first < 0:\n                first = index\n            last = index\n    if first < 0:\n        return 0\n    return last - first\n\n\ndef priority(column_code, n, k, d):\n    \"\"\"\n    Score a candidate free column using only static instance information.\n\n    The evaluator keeps legality exact; this function only defines a global ranking.\n    \"\"\"\n    r = _row_count(n, k)\n    digits = _decode_column(column_code, r)\n    weight = _support_weight(digits)\n    target_weight = _target_weight(n, k, d)\n\n    weight_score = -abs(weight - target_weight)\n    symmetry_score = _center_symmetry_score(digits)\n    run_score = _run_count_score(digits)\n    balance_score = _symbol_balance_score(digits)\n    span_score = _nonzero_span_score(digits)\n    endpoint_bonus = 0.5 * (digits[0] != 0) + 0.5 * (digits[-1] != 0)\n\n    return (\n        2.5 * weight_score\n        + 0.8 * symmetry_score\n        + 0.4 * balance_score\n        + 0.35 * run_score\n        + 0.15 * span_score\n        + endpoint_bonus\n    )\n\n\n# EVOLVE-BLOCK-END\n\nfrom search_core import DEFAULT_INSTANCE, evaluate_priority_function, instance_from_env\n\n\ndef get_priority_function():\n    \"\"\"Stable entry point for tests and manual inspection.\"\"\"\n    return priority\n\n\ndef run_baseline_suite():\n    \"\"\"Evaluate the current priority function on one configured instance.\"\"\"\n    return evaluate_priority_function(priority, instance_from_env())\n\n\nif __name__ == \"__main__\":\n    configured_instance = instance_from_env()\n    result = run_baseline_suite()\n    print(\"Default instance:\", DEFAULT_INSTANCE)\n    print(\"Configured instance:\", configured_instance)\n    print(\"Metrics:\", result.metrics)\n    print(\"Artifacts:\", result.artifacts)\n\n```\nKey features: Performs well on combined_score (1.0000), Performs well on success_rate (1.0000), Performs well on avg_progress (1.0000), Performs well on constructed_columns (8.0000), Performs well on target_columns (8.0000), Performs well on target_distance (5.0000), Performs well on n (15.0000), Performs well on k (8.0000), Performs well on evaluation_time_seconds (0.1285)\n\n\n\n# Current Program\n```python\n\"\"\"Baseline static priority heuristic for FunSearch-style ternary matrix construction.\"\"\"\n\n\n# EVOLVE-BLOCK-START\ndef _row_count(n, k):\n    return n - k\n\n\ndef _target_weight(n, k, d):\n    r = _row_count(n, k)\n    return max(d - 1, r // 2 + 1)\n\n\ndef _decode_column(column_code, r):\n    digits = []\n    value = column_code\n    for _ in range(r):\n        digits.append(value % 3)\n        value //= 3\n    return digits\n\n\ndef _support_weight(digits):\n    return sum(1 for digit in digits if digit)\n\n\ndef _center_symmetry_score(digits):\n    r = len(digits)\n    left = 0\n    right = 0\n    for i in range(r // 2):\n        if digits[i]:\n            left += 1\n        if digits[r - 1 - i]:\n            right += 1\n    return -abs(left - right)\n\n\ndef _run_count_score(digits):\n    bits = \"\".join(str(digit) for digit in digits)\n    runs = 1\n    for i in range(1, len(bits)):\n        if bits[i] != bits[i - 1]:\n            runs += 1\n    return runs\n\n\ndef _symbol_balance_score(digits):\n    ones = digits.count(1)\n    twos = digits.count(2)\n    return -abs(ones - twos)\n\n\ndef _nonzero_span_score(digits):\n    first = -1\n    last = -1\n    for index, digit in enumerate(digits):\n        if digit:\n            if first < 0:\n                first = index\n            last = index\n    if first < 0:\n        return 0\n    return last - first\n\n\ndef priority(column_code, n, k, d):\n    \"\"\"\n    Score a candidate free column using only static instance information.\n\n    The evaluator keeps legality exact; this function only defines a global ranking.\n    \"\"\"\n    r = _row_count(n, k)\n    digits = _decode_column(column_code, r)\n    weight = _support_weight(digits)\n    target_weight = _target_weight(n, k, d)\n\n    weight_score = -abs(weight - target_weight)\n    symmetry_score = _center_symmetry_score(digits)\n    run_score = _run_count_score(digits)\n    balance_score = _symbol_balance_score(digits)\n    span_score = _nonzero_span_score(digits)\n    endpoint_bonus = 0.5 * (digits[0] != 0) + 0.5 * (digits[-1] != 0)\n\n    return (\n        2.5 * weight_score\n        + 0.8 * symmetry_score\n        + 0.4 * balance_score\n        + 0.35 * run_score\n        + 0.15 * span_score\n        + endpoint_bonus\n    )\n\n\n# EVOLVE-BLOCK-END\n\nfrom search_core import DEFAULT_INSTANCE, evaluate_priority_function, instance_from_env\n\n\ndef get_priority_function():\n    \"\"\"Stable entry point for tests and manual inspection.\"\"\"\n    return priority\n\n\ndef run_baseline_suite():\n    \"\"\"Evaluate the current priority function on one configured instance.\"\"\"\n    return evaluate_priority_function(priority, instance_from_env())\n\n\nif __name__ == \"__main__\":\n    configured_instance = instance_from_env()\n    result = run_baseline_suite()\n    print(\"Default instance:\", DEFAULT_INSTANCE)\n    print(\"Configured instance:\", configured_instance)\n    print(\"Metrics:\", result.metrics)\n    print(\"Artifacts:\", result.artifacts)\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: avg_progress, constructed_columns\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["Looking at the current program, it achieves perfect fitness (1.0000) for the given parameters (n=15, k=8, d=5, r=7). The goal is to maintain this performance while potentially simplifying the code (as noted in the focus areas about code length exceeding 500 characters).\n\nThe current priority function has several components that may be redundant or could be simplified without losing performance:\n\n1. `_center_symmetry_score` and `_nonzero_span_score` are somewhat related - both deal with the spatial distribution of non-zero entries\n2. `_run_count_score` might not be as relevant for GF(3) properties\n3. The weighting coefficients could be tuned\n\nKey observations for improvement:\n- For r=7, we need columns that work well together to maintain d-1=4 column independence\n- The target weight of max(4, 4) = 4 is optimal for this case\n- Symbol balance (1 vs 2) matters for linear independence properties\n\nHere's my proposed improvement that simplifies while maintaining performance:\n\n<<<<<<< SEARCH\ndef _center_symmetry_score(digits):\n    r = len(digits)\n    left = 0\n    right = 0\n    for i in range(r // 2):\n        if digits[i]:\n            left += 1\n        if digits[r - 1 - i]:\n            right += 1\n    return -abs(left - right)\n\n\ndef _run_count_score(digits):\n    bits = \"\".join(str(digit) for digit in digits)\n    runs = 1\n    for i in range(1, len(bits)):\n        if bits[i] != bits[i - 1]:\n            runs += 1\n    return runs\n\n\ndef _symbol_balance_score(digits):\n    ones = digits.count(1)\n    twos = digits.count(2)\n    return -abs(ones - twos)\n\n\ndef _nonzero_span_score(digits):\n    first = -1\n    last = -1\n    for index, digit in enumerate(digits):\n        if digit:\n            if first < 0:\n                first = index\n            last = index\n    if first < 0:\n        return 0\n    return last - first\n\n\ndef priority(column_code, n, k, d):\n    \"\"\"\n    Score a candidate free column using only static instance information.\n\n    The evaluator keeps legality exact; this function only defines a global ranking.\n    \"\"\"\n    r = _row_count(n, k)\n    digits = _decode_column(column_code, r)\n    weight = _support_weight(digits)\n    target_weight = _target_weight(n, k, d)\n\n    weight_score = -abs(weight - target_weight)\n    symmetry_score = _center_symmetry_score(digits)\n    run_score = _run_count_score(digits)\n    balance_score = _symbol_balance_score(digits)\n    span_score = _nonzero_span_score(digits)\n    endpoint_bonus = 0.5 * (digits[0] != 0) + 0.5 * (digits[-1] != 0)\n\n    return (\n        2.5 * weight_score\n        + 0.8 * symmetry_score\n        + 0.4 * balance_score\n        + 0.35 * run_score\n        + 0.15 * span_score\n        + endpoint_bonus\n    )\n=======\ndef _symbol_balance_score(digits):\n    ones = digits.count(1)\n    twos = digits.count(2)\n    return -abs(ones - twos)\n\n\ndef _nonzero_span_score(digits):\n    first = -1\n    last = -1\n    for index, digit in enumerate(digits):\n        if digit:\n            if first < 0:\n                first = index\n            last = index\n    if first < 0:\n        return 0\n    return last - first\n\n\ndef priority(column_code, n, k, d):\n    \"\"\"\n    Score a candidate free column using only static instance information.\n\n    The evaluator keeps legality exact; this function only defines a global ranking.\n    \"\"\"\n    r = _row_count(n, k)\n    digits = _decode_column(column_code, r)\n    weight = _support_weight(digits)\n    target_weight = _target_weight(n, k, d)\n\n    weight_score = -abs(weight - target_weight)\n    balance_score = _symbol_balance_score(digits)\n    span_score = _nonzero_span_score(digits)\n    endpoint_bonus = 0.5 * (digits[0] != 0) + 0.5 * (digits[-1] != 0)\n\n    return (\n        2.5 * weight_score\n        + 0.5 * balance_score\n        + 0.2 * span_score\n        + endpoint_bonus\n    )\n>>>>>>> REPLACE\n\nThis change:\n1. Removes `_center_symmetry_score` - it was redundant with `_nonzero_span_score` and `endpoint_bonus`\n2. Removes `_run_count_score` - run patterns in base-3 representation have weak theoretical justification for GF(3) linear independence\n3. Simplifies the weighting scheme while keeping the most impactful components:\n   - Weight targeting (most important for d-1 independence)\n   - Symbol balance (important for GF(3) linear combinations)\n   - Span score (helps with diversity of column shapes)\n   - Endpoint bonus (promotes spread-out columns)\n4. Reduces code length significantly while maintaining the same fitness\n\nThe removed features were likely providing marginal or no benefit for this specific parameter set, and the simplified version should maintain the perfect fitness of 1.0000 while being cleaner and more efficient."]}}, "artifacts_json": "{\"instance\": \"{\\\"d\\\": 5, \\\"k\\\": 8, \\\"n\\\": 15, \\\"name\\\": \\\"instance_[15,8,5]\\\", \\\"restarts\\\": 3}\", \"search_result\": \"{\\\"added_free_columns\\\": 8, \\\"blocked_candidates\\\": 28, \\\"candidate_count\\\": 904, \\\"chosen_weights\\\": [4, 4, 4, 4, 4, 4, 4, 4], \\\"restart\\\": 0, \\\"selected_free_columns\\\": [\\\"1202001\\\", \\\"1020012\\\", \\\"1020201\\\", \\\"1210002\\\", \\\"1001022\\\", \\\"1002102\\\", \\\"1100202\\\", \\\"1200210\\\"], \\\"success\\\": true, \\\"target_free_columns\\\": 8}\", \"top_ranked_columns\": \"[{\\\"column\\\": \\\"1202001\\\", \\\"score\\\": 2.2}, {\\\"column\\\": \\\"1200021\\\", \\\"score\\\": 2.2}, {\\\"column\\\": \\\"1020012\\\", \\\"score\\\": 2.2}, {\\\"column\\\": \\\"1000212\\\", \\\"score\\\": 2.2}, {\\\"column\\\": \\\"1200012\\\", \\\"score\\\": 2.2}, {\\\"column\\\": \\\"1020201\\\", \\\"score\\\": 2.2}, {\\\"column\\\": \\\"1010202\\\", \\\"score\\\": 2.2}, {\\\"column\\\": \\\"1220001\\\", \\\"score\\\": 2.2}, {\\\"column\\\": \\\"1200201\\\", \\\"score\\\": 2.2}, {\\\"column\\\": \\\"1210002\\\", \\\"score\\\": 2.2}]\", \"blocked_weight_histogram\": \"{\\\"4\\\": 28}\"}", "artifact_dir": null, "embedding": null}