# Shared LLM configuration for binary linear-code search.

llm:
  api_base: "https://api.deepseek.com/v1"
  models:
    - name: "deepseek-chat"
      weight: 1.0
  temperature: 0.7
  max_tokens: 32768
  timeout: 600
